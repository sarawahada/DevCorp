<form method="post" action="{{ path('app_promo_delete', {'idPromo': promo.idPromo}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ promo.idPromo) }}">
    <button class="btn">Delete</button>
</form>
